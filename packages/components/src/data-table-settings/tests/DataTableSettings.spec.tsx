import { mount } from '@vue/test-utils'
import { ZDataTableSettings } from '../index'

describe('z-details-view', () => {
  it('should work with import on demand', () => {
    mount(ZDataTableSettings)
  })
})
